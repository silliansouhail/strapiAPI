'use strict';

/**
 * reclamation-of controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::reclamation-of.reclamation-of');
